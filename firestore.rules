rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to application collections
    // Application-level RBAC, PIN verification, and invariants are enforced by the server and client auth layer
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
